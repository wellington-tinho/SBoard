import { render } from '@testing-library/react'
import { CreateRequest } from "../components/Aside/CreateRequest";

describe('CreateRequest', () => {
  test('sum',()=>{
    expect(1+2).toBe(3);
  }
  )
}
)

// Language: typescript

